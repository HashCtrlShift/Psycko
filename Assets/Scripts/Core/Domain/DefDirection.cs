namespace Psycko.Core.Domain
{
    /// <summary>
    /// Direction de jeu (sens de l'ordre des joueurs), inversée par le Valet.
    /// </summary>
    public enum PlayDirection
    {
        Clockwise = 0,
        CounterClockwise = 1
    }
}